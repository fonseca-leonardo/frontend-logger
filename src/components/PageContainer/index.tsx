import styled from 'styled-components';

export const PageContainer = styled.div`
    width: 100%;

    padding: 4px 16px;

    max-height: 812px;

    overflow-y: hidden;

    z-index: 1;

    & > div > h1 {
        margin: 0;

        margin-bottom: 16px;
    }
`;
